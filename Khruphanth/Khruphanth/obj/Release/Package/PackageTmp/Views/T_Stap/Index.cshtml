@model IEnumerable<Khruphanth.Models.T_Stap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ST_StepName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ST_StepName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StepID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StepID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StepID })
        </td>
    </tr>
}

</table>
